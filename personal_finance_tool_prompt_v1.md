# 個人資金管理ツール 設計仕様書 v1

> プロジェクト名: **人生奪還PJ 資金管理ツール**
> 成果物ファイル: `人生奪還PJ_資金管理ツール_v1.xlsm`（マクロ有効ブック）
>
> 本書は本プロジェクト全体の設計仕様書です。元の `personal_finance_tool_prompt_v1.md` が
> 参照できなかったため、初回投入プロンプトに記載された要件と一般的な家計管理設計に基づき
> 起こしたものです。以降のフェーズはこの仕様に従って実装します。

---

## 1. プロジェクト概要

家計の入出金を Excel + VBA で一元管理し、資産形成の進捗（目標純資産額 1億円 / 2033年8月達成）を
可視化する個人向け資金管理ツール。手入力とCSV取込の両方を入口とし、取引台帳へ集約、
月次・年次で集計・分析・アラートを行う。

## 2. シート構成（全10シート）

| # | シート名 | CodeName | タブ色 | 役割 | 導入フェーズ |
|---|----------|----------|--------|------|------|
| 1 | 設定 | `shtConfig` | グレー | 各種マスタ・システム設定・名前付き範囲の起点 | Phase 1 |
| 2 | 手入力 | `shtManual` | 青 | 日次の入出金を手入力し台帳へ転記 | Phase 1 |
| 3 | CSV取込 | `shtImport` | 緑 | 銀行/カードのCSVを貼付け→整形→取込 | Phase 2 |
| 4 | 取引台帳 | `shtLedger` | 濃紺 | 全取引の正本（append-only の明細） | Phase 2 |
| 5 | 月次集計 | `shtMonthly` | 橙 | 大分類×月のクロス集計 | Phase 3 |
| 6 | 年次集計 | `shtYearly` | 橙 | 年度サマリ・収支推移 | Phase 3 |
| 7 | 予算管理 | `shtBudget` | 黄 | 費目別予算と実績対比・浪費アラート | Phase 4 |
| 8 | 資産管理 | `shtAsset` | 紫 | 口座・資産残高と純資産推移 | Phase 5 |
| 9 | ダッシュボード | `shtDashboard` | 赤 | KPI・グラフ・目標進捗の一覧 | Phase 6 |
| 10 | ヘルプ | `shtHelp` | 白 | 操作手順・命名規則・変更履歴 | Phase 7 |

> Phase 1 で作成するのは **設定** と **手入力** の2シートのみ（タブ色: 設定=グレー / 手入力=青）。

## 3. データフロー

```
[手入力シート] ─┐
                ├─(台帳転記)→ [取引台帳] ─→ [月次集計]/[年次集計] ─→ [ダッシュボード]
[CSV取込シート] ─┘        ▲                          │
                          │                          └→ [予算管理]（浪費アラート）
                    [設定シート]（カテゴリ/支払方法/分類ルール/システム設定）
                          │
                    [資産管理]（残高・純資産推移）→ [ダッシュボード]
```

- **設定シート**が全処理の参照元（マスタ・閾値・目標値）。
- 入力は**手入力**と**CSV取込**の2経路。いずれも最終的に**取引台帳**へ1明細=1行で集約。
- 取引台帳を単一の正本とし、集計・分析・アラート・ダッシュボードは台帳を集計して生成。

## 4. カテゴリマスタ（大分類 / 中分類 / 収支区分）

| 大分類 | 中分類 | 収支区分 |
|--------|--------|----------|
| 食費 | 食料品 / 外食 / カフェ・嗜好品 | 支出 |
| 住居費 | 家賃・ローン / 管理費・修繕積立 / 火災保険 | 支出 |
| 水道光熱費 | 電気 / ガス / 水道 | 支出 |
| 通信費 | 携帯電話 / インターネット / 放送・その他 | 支出 |
| 交通費 | 電車・バス / ガソリン / タクシー | 支出 |
| 日用品 | 消耗品 / 衛生用品 | 支出 |
| 医療・健康 | 病院・薬 / 医療保険 / フィットネス | 支出 |
| 教育・教養 | 書籍 / 学習・講座 / 新聞・雑誌 | 支出 |
| 娯楽・交際費 | 趣味・レジャー / 交際費 / 旅行 | 支出 |
| 被服・美容 | 衣服 / 美容・理容 / 化粧品 | 支出 |
| 保険 | 生命保険 / 損害保険 | 支出 |
| 税金・社会保険 | 所得税・住民税 / 社会保険料 | 支出 |
| 特別費 | 冠婚葬祭 / 家電・家具 / その他特別費 | 支出 |
| 給与収入 | 給与 / 賞与 | 収入 |
| 事業・副業収入 | 事業所得 / 副業 | 収入 |
| 資産収入 | 配当・利息 / 不動産収入 | 収入 |
| その他収入 | 臨時収入 / 還付金 | 収入 |

## 5. 支払方法マスタ

| 支払方法 | 種別 |
|----------|------|
| 現金 | 現金 |
| メガバンク口座引落 | 口座 |
| クレジットカード | カード |

## 6. システム設定

| 設定項目 | 初期値 |
|----------|--------|
| 対象年度 | 2026 |
| 浪費アラート閾値 | 15%（0.15） |
| 目標純資産額 | 100,000,000 円 |
| 達成期限 | 2033/8/31 |

## 7. 設定シート レイアウト（列割当）

| 領域 | 列 | ヘッダー |
|------|----|----------|
| カテゴリマスタ | A〜D | 大分類 / 中分類 / 収支区分 / メモ |
| 支払方法マスタ | F〜G | 支払方法 / 種別 |
| CSV列マッピング（Phase 2 用・枠のみ） | I〜M | 論理名 / CSV列見出し / 列番号 / データ型 / 備考 |
| 自動分類ルール（Phase 2 用・サンプル5件） | O〜Q | キーワード / 大分類 / 中分類 |
| システム設定 | S〜T | 設定項目 / 値 |
| 大分類リスト（入力補助ヘルパー） | V | 大分類 |

## 8. 手入力シート レイアウト（A〜H列）

| 列 | 項目 | 入力補助・規則 |
|----|------|----------------|
| A | 日付 | 新規行はデフォルト今日。Date型・必須 |
| B | 大分類 | `大分類リスト` からドロップダウン |
| C | 中分類 | B列に連動（Worksheet_Change で動的生成） |
| D | 摘要 | 任意テキスト |
| E | 金額 | 正の整数・必須 |
| F | 支払方法 | `支払方法リスト` からドロップダウン |
| G | 収支区分 | 大分類から自動判定（VBA） |
| H | 台帳転記済 | 転記時に「済」を記録 |

- 上部に「取引台帳へ転記」ボタンを配置。Phase 1 では転記先シート存在チェックをスキップし、
  ボタンとプロシージャの枠のみ用意する。

## 9. 名前付き範囲

| 名前 | 参照 |
|------|------|
| `カテゴリマスタ` | 設定!$A$2:$D$(最終行) |
| `支払方法マスタ` | 設定!$F$2:$G$4 |
| `支払方法リスト` | 設定!$F$2:$F$4 |
| `CSV列マッピング` | 設定!$I$2:$M$21 |
| `自動分類ルール` | 設定!$O$2:$Q$6 |
| `システム設定` | 設定!$S$2:$T$5 |
| `大分類リスト` | 設定!$V$2:$V$(最終行) |
| `対象年度` / `浪費アラート閾値` / `目標純資産額` / `達成期限` | 設定!$T$2 〜 $T$5 |

## 10. VBAモジュール構成

| モジュール | 種別 | 役割 |
|-----------|------|------|
| `modConfig` | 標準 | 定数定義・設定値/マスタの読込関数 |
| `modManualEntry` | 標準 | 手入力の入力補助・台帳転記（Phase 1 は枠） |
| `modUtils` | 標準 | 共通ユーティリティ（日付・書式）・テストデータ生成 |
| `shtManual`（手入力） | シートモジュール | Worksheet_Change（連動DD・既定日付・収支区分自動判定） |

（Phase 2 以降で `modImport` / `modLedger` / `modAggregate` / `modBudget` / `modAsset` / `modDashboard` を追加）

## 11. VBA設計ルール（全フェーズ共通・必ず守ること）

- 全モジュール先頭に `Option Explicit`。
- 全 Public プロシージャに `On Error GoTo ErrHandler` を設置。
- 処理開始時 `Application.ScreenUpdating = False` / `Application.Calculation = xlCalculationManual`。
- 処理終了時に必ず復元。`ErrHandler` 内でも復元すること。
- 変数名はキャメルケース、定数は大文字スネークケース。
- シート参照は CodeName を使用。
- コメントは日本語で記載。

## 12. フェーズ計画（全8フェーズ）

| フェーズ | 内容 |
|---------|------|
| Phase 1 | 基盤構築（設定シート＋手入力シート、modConfig/modManualEntry/modUtils） |
| Phase 2 | CSV取込・取引台帳（modImport/modLedger、自動分類ルール適用、台帳転記の本接続） |
| Phase 3 | 月次・年次集計（modAggregate、クロス集計・推移） |
| Phase 4 | 予算管理・浪費アラート（modBudget、閾値判定） |
| Phase 5 | 資産管理・純資産推移（modAsset） |
| Phase 6 | ダッシュボード（modDashboard、KPI・グラフ・目標進捗） |
| Phase 7 | ヘルプ整備・操作性改善・入力バリデーション強化 |
| Phase 8 | 総合テスト・パフォーマンス最適化・リリース |

---

## 13. Phase 2 実装メモ（CSV取込・取引台帳）

既存ブックへの**完全追加型**で実装（Phase 1 のモジュールは無変更）。

### 追加シート
- **取引台帳**（`SHEET_LEDGER`, 濃紺）: A取引ID / B日付 / C大分類 / D中分類 / E摘要 /
  F金額 / G支払方法 / H収支区分 / I入力元 / J取込日時 / K重複キー。
  動的名前付き範囲 `取引台帳`（OFFSET+COUNTA）を定義。
- **CSV取込**（`SHEET_IMPORT`, 緑）: 取込プレビュー（取込○/日付/摘要/金額/収支区分/
  大分類/中分類/支払方法/種別/元行/重複?）＋ボタン4種。

### 追加VBAモジュール
- `modLedger`: 台帳追記 `AppendRecords`、取引ID採番 `NextTransactionId`、
  重複キー `BuildDupKey`、手入力転記の本接続 `TransferManualToLedger`。
- `modImport`: `ImportAccountCSV`/`ImportCardCSV`（ADODB.Streamで文字コード対応、
  CSV引用符対応パーサ、金額/日付の柔軟解釈）、自動分類 `Classify`、
  台帳取込 `CommitPreviewToLedger`、`ClearPreview`。
- `modSetup`: `Setup_Phase2`（シート・ボタン・名前付き範囲・マッピング/ルール初期化。冪等）。

### CSV列マッピング（設定シート I〜L 列に schema 変更）
`種別 / 項目 / 値 / 備考`。口座は 出金列→支出・入金列→収入、カードは全件支出。
文字コード・列番号は実CSVに合わせて編集可能。

### 重複排除
`日付＋金額＋摘要先頭20字＋支払方法` をキーに台帳K列で判定（再取込しても安全）。

詳細な導入手順は `docs/SETUP_PHASE2.md` を参照。
